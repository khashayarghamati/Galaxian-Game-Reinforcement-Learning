name: gal3
channels:
  - defaults
dependencies:
  - pip=23.3
  - python=3.11.5
  - pip:
      - torch==1.13.0
      - neptune
      - gymnasium[atari,accept-rom-license]
prefix: /opt/miniconda3/envs/gal3
